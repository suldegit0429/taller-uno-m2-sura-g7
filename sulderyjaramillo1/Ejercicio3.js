let historial = ["google.com", "youtube.com", "facebook.com"];

// Eliminar el último elemento (facebook.com)
historial.pop();

// Eliminar el siguiente último (youtube.com)
historial.pop();

console.log(historial);